{
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build --emptyOutDir",
    "serve": "vite preview",
    "postbuild": "node generate-pdf.js"
  },
  "devDependencies": {
    "puppeteer": "^10.0.0",
    "vite": "^3.0.0"
  }
}
